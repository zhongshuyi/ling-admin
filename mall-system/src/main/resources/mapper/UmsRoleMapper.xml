<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.mall.system.mapper.UmsRoleMapper">

    <sql id="selectRoleVo">
        select distinct r.role_id,
                        r.role_name,
                        r.role_key,
                        r.role_sort,
                        r.data_scope,
                        r.menu_check_strictly,
                        r.dept_check_strictly,
                        r.status,
                        r.del_flag,
                        r.create_time,
                        r.remark
        from ums_role r
                 left join ums_user_role ur on ur.role_id = r.role_id
                 left join ums_admin u on u.user_id = ur.user_id
                 left join ums_dept d on u.dept_id = d.dept_id
    </sql>

    <select id="selectRoleListByUserId" resultType="com.mall.common.core.domain.entity.UmsRole" parameterType="long">
        <include refid="selectRoleVo"/>
        WHERE r.del_flag = '0' and ur.user_id = #{userId}
    </select>
</mapper>
